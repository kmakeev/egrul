# Управление системой EGRUL/EGRIP

## Команды запуска и остановки

### 🚀 Запуск системы

#### Запуск всей системы (рекомендуется)
```bash
make up
```
Автоматически запускает в правильном порядке:
1. ClickHouse кластер (6 нод + 3 Keeper)
2. Базовые сервисы (PostgreSQL, Redis, Elasticsearch, Kafka, MinIO)
3. Прикладные сервисы (API Gateway, Search, Frontend, Change Detection, Notification, Sync)
4. Подключает сервисы к кластерной сети

**Доступные сервисы после запуска:**
- Frontend: http://localhost:3000
- GraphQL Playground: http://localhost:8080/playground
- MailHog UI: http://localhost:8025
- MinIO Console: http://localhost:9011
- Adminer (PostgreSQL): http://localhost:8090
- RedisInsight: http://localhost:8091

#### Запуск только мониторинга
```bash
make monitoring-up
```
Запускает систему мониторинга:
- **Prometheus**: http://localhost:9090 - метрики и alerts
- **Grafana**: http://localhost:3001 (admin/admin) - дашборды
- **cAdvisor**: http://localhost:8085 - метрики контейнеров
- **Loki**: http://localhost:3100 - централизованные логи
- **Promtail**: автоматический сбор логов

**Доступные дашборды в Grafana:**
- API Gateway Dashboard - детальные метрики API
- Search Service Dashboard - производительность поиска
- Change Detection Dashboard - мониторинг изменений
- Notification Service Dashboard - статус уведомлений
- ClickHouse Cluster Dashboard - состояние кластера
- EGRUL Logs Dashboard - централизованные логи
- EGRUL Services Overview - обзор всех сервисов

#### Запуск только ClickHouse кластера
```bash
make cluster-up
```
Запускает только ClickHouse кластер (6 нод + 3 Keeper + Prometheus).

### 🛑 Остановка системы

#### Остановка только мониторинга
```bash
make monitoring-down
```
**Что делает:**
- Останавливает: Prometheus, Grafana, cAdvisor, Loki, Promtail
- **НЕ останавливает**: API Gateway, Frontend, ClickHouse, PostgreSQL и другие сервисы
- Контейнеры остановлены, но **не удалены** (можно перезапустить через `make monitoring-up`)

**Когда использовать:**
- Мониторинг не нужен, но приложение должно работать
- Освободить ресурсы CPU/RAM без остановки основных сервисов
- Временно отключить мониторинг для отладки

#### Остановка только ClickHouse кластера
```bash
make cluster-down
```
**Что делает:**
- Останавливает: 6 нод ClickHouse + 3 Keeper + Prometheus кластера
- **НЕ останавливает**: остальные сервисы из docker-compose.yml

#### Остановка ВСЕЙ системы
```bash
make down
```
**Что делает:**
- Останавливает **ВСЕ** контейнеры проекта из всех профилей:
  - `--profile full` (Kafka, MinIO, Change Detection, Notification, все основные сервисы)
  - `--profile monitoring` (Prometheus, Grafana, cAdvisor, Loki, Promtail)
  - `--profile tools` (Adminer, RedisInsight)
  - `--profile cluster` (ClickHouse кластер)
- Контейнеры **удаляются** (не просто останавливаются)
- Volumes и данные **сохраняются** (не удаляются)

**Когда использовать:**
- Полная остановка всей инфраструктуры
- Перед обновлением docker-compose конфигурации
- Для полной очистки перед новым запуском

## Проверка статуса

### Статус всех сервисов
```bash
docker compose ps
```

### Статус мониторинга
```bash
make monitoring-status
```
Показывает статус Prometheus, Grafana, cAdvisor, Loki, Promtail.

### Статус ClickHouse кластера
```bash
make cluster-ps
# или
make cluster-verify
```

### Логи сервисов
```bash
# Все сервисы
make docker-logs

# Только мониторинг
make monitoring-logs

# Только кластер
make cluster-logs

# Конкретный сервис
docker compose logs -f api-gateway
docker compose logs -f grafana
```

## Перезапуск сервисов

### Перезапуск мониторинга
```bash
make monitoring-down
make monitoring-up
```

### Перезапуск кластера
```bash
make cluster-restart
```

### Перезапуск конкретного сервиса
```bash
docker compose restart api-gateway
docker compose restart grafana
docker compose restart prometheus
```

## Примеры использования

### Пример 1: Отключить мониторинг для экономии ресурсов
```bash
# Остановить только мониторинг
make monitoring-down

# API, Frontend и БД продолжают работать
curl http://localhost:8080/health  # ✅ работает

# Позже включить обратно
make monitoring-up
```

### Пример 2: Полная остановка всего
```bash
# Остановить всю инфраструктуру
make down

# Проверить что все остановлено
docker compose ps  # должно быть пусто
```

### Пример 3: Перезапуск после изменений в docker-compose
```bash
# Остановить всё
make down

# Пересобрать образы
make docker-build

# Запустить заново
make up
```

### Пример 4: Отладка без мониторинга
```bash
# Запустить только основные сервисы (без мониторинга)
make cluster-up
docker compose --profile full up -d postgres redis elasticsearch kafka api-gateway frontend

# Мониторинг не запущен, ресурсы освобождены
```

## Профили Docker Compose

Система использует профили для группировки сервисов:

- **default** (без флага): PostgreSQL, Redis, Elasticsearch, API Gateway, Frontend, Search Service
- **full**: Все default + Kafka, MinIO, Change Detection, Notification, Sync, Adminer, RedisInsight
- **monitoring**: Prometheus, Grafana, cAdvisor, Loki, Promtail
- **tools**: Adminer, RedisInsight, MailHog
- **cluster**: ClickHouse кластер (6 нод + 3 Keeper)

### Примеры использования профилей

```bash
# Запуск только базовых сервисов (без full/monitoring)
docker compose up -d

# Запуск всех сервисов + мониторинг
docker compose --profile full --profile monitoring up -d

# Запуск только инструментов
docker compose --profile tools up -d
```

## Команды очистки

### Очистка всех данных (volumes)
```bash
make docker-clean
```
**ВНИМАНИЕ**: Удаляет ВСЕ volumes (данные PostgreSQL, ClickHouse, Kafka, MinIO будут потеряны!)

### Очистка только ClickHouse кластера
```bash
make cluster-clean
```
Удаляет volumes кластера, но сохраняет данные основных сервисов.

## Быстрая справка

| Команда | Что делает | Удаляет контейнеры? | Удаляет данные? |
|---------|------------|---------------------|-----------------|
| `make up` | Запустить всю систему | - | - |
| `make down` | Остановить ВСЁ | ✅ Да | ❌ Нет |
| `make monitoring-down` | Остановить только мониторинг | ❌ Нет (stop) | ❌ Нет |
| `make cluster-down` | Остановить кластер ClickHouse | ✅ Да | ❌ Нет |
| `make docker-clean` | Полная очистка | ✅ Да | ✅ Да (volumes) |
| `docker compose restart X` | Перезапуск сервиса X | ❌ Нет | ❌ Нет |

## Решение проблем

### Проблема: "make down" не останавливает мониторинг
**Решение**: Обновлен Makefile, теперь `make down` останавливает все профили (`--profile full --profile monitoring --profile tools`).

### Проблема: После "make monitoring-down" нельзя запустить мониторинг снова
**Решение**: `monitoring-down` теперь использует `stop` вместо `down`, контейнеры не удаляются.

### Проблема: Контейнеры "orphan" при запуске
```bash
# Удалить orphan контейнеры
docker compose down --remove-orphans
```

### Проблема: Порт занят
```bash
# Найти процесс на порту 3001 (Grafana)
lsof -i :3001

# Остановить конфликтующий сервис
docker compose stop grafana
```
