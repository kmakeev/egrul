# Frontend - ЕГРЮЛ/ЕГРИП

Веб-интерфейс для системы поиска и аналитики по ЕГРЮЛ/ЕГРИП.

## Технологический стек

- **Next.js 14** с App Router
- **TypeScript** (strict mode)
- **Tailwind CSS** для стилизации
- **Shadcn/ui** компоненты
- **TanStack Query** для data fetching
- **Zustand** для глобального состояния
- **Zod** для валидации
- **React Hook Form** для работы с формами

## Структура проекта

```
frontend/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (auth)/             # Группа маршрутов для аутентификации
│   │   │   ├── login/
│   │   │   └── register/
│   │   ├── (dashboard)/        # Группа маршрутов для дашборда
│   │   │   ├── search/
│   │   │   ├── company/[ogrn]/
│   │   │   ├── entrepreneur/[ogrnip]/
│   │   │   ├── analytics/
│   │   │   ├── watchlist/
│   │   │   └── settings/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   └── providers.tsx
│   ├── components/
│   │   ├── ui/                 # Shadcn/ui компоненты
│   │   ├── search/
│   │   ├── company/
│   │   ├── analytics/
│   │   └── layout/
│   ├── lib/
│   │   ├── api/                # API клиенты
│   │   ├── utils/              # Утилиты
│   │   └── validations/        # Zod схемы
│   ├── hooks/                  # Кастомные хуки
│   ├── store/                  # Zustand сторы
│   └── types/                  # TypeScript типы
├── public/                     # Статические файлы
└── package.json
```

## Установка

```bash
# Установка зависимостей
npm install
# или
pnpm install
# или
yarn install
```

## Настройка окружения

Создайте файл `.env.local` на основе `.env.example`:

```bash
cp .env.example .env.local
```

Отредактируйте переменные окружения:

```env
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
NODE_ENV=development
```

## Разработка

```bash
# Запуск dev сервера
npm run dev

# Проверка типов
npm run type-check

# Линтинг
npm run lint

# Форматирование кода
npm run format
```

## Сборка

```bash
# Production сборка
npm run build

# Запуск production сервера
npm start
```

## Git Hooks

Проект настроен с Husky для pre-commit хуков:

- Автоматический линтинг и форматирование измененных файлов
- Проверка типов TypeScript

## Компоненты UI

Проект использует Shadcn/ui компоненты:

- **Button** - кнопки с различными вариантами
- **Input** - поля ввода
- **Select** - выпадающие списки
- **Card** - карточки контента
- **Table** - таблицы данных
- **Dialog** - модальные окна
- **Form** - формы с валидацией
- **Toast** - уведомления
- **Tabs** - вкладки
- **Accordion** - аккордеоны

## API клиент

API клиент находится в `src/lib/api/client.ts` и предоставляет методы для:

- Поиска юридических лиц
- Поиска индивидуальных предпринимателей
- Получения детальной информации по ОГРН/ОГРНИП
- Глобального поиска

## Состояние приложения

Глобальное состояние управляется через Zustand:

- `auth-store` - состояние аутентификации
- `search-store` - состояние поиска и фильтров

## Валидация

Валидация форм выполняется с помощью Zod схем в `src/lib/validations/`:

- `common.ts` - общие схемы (ОГРН, ИНН, КПП)
- `auth.ts` - схемы для аутентификации

## 🎨 Цветовые темы

В проекте доступно **7 готовых цветовых схем** на выбор:

- 🌙 **Темная** (текущая) - классическая темная тема
- 💼 **Синяя профессиональная** - для бизнес-приложений
- 🌿 **Зеленая экологичная** - свежая природная тема
- 🔥 **Оранжевая энергичная** - для динамичных интерфейсов
- 💜 **Фиолетовая премиум** - роскошная элегантная тема
- ☀️ **Светлая** - классическая светлая тема
- ❤️ **Красная активная** - смелая для важных данных

### Как изменить тему:

1. Откройте файл `THEMES.md` для подробных инструкций
2. Выберите тему из `src/styles/themes.ts`
3. Скопируйте значения цветов в `src/app/globals.css`
4. Пересоберите проект

Подробнее: [THEMES.md](./THEMES.md)

## Лицензия

MIT

